# Tools

Runtime:

- Next.js on port `3000`.
- PM2 through `pm2-runtime`.
- SQLite via `better-sqlite3`.

Pinata route:

- Public path route at `/app`.
- Container port `3000`.

Local commands:

```bash
npm run dev
npm run build
npm start
```
